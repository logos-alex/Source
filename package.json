{
  "name": "source",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "eleventy --serve --port=5000 --pathprefix=/Source/",
    "build": "eleventy --pathprefix=/Source/ && pagefind --site _site --output-path _site/pagefind",
    "deploy": "gh-pages -d _site",
    "test": "echo \"Error: no test specified\" && exit 1",
    "check:frontmatter": "node scripts/verify-frontmatter.mjs",
    "ci:verify": "pnpm run check:frontmatter && pnpm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.2",
    "gh-pages": "^6.3.0",
    "luxon": "^3.7.2",
    "pagefind": "^1.4.0"
  }
}