---
layout: base.njk
title: "כתבים ממקור לטיני"
description: "חיבורים שהשתמרו בתרגום לטיני מן העולם הנוצרי־מערבי."
permalink: /texts/latin/
---
<div class="text-main library-page">
  <header class="text-header">
    <p class="text-header__eyebrow">לפי שפת מקור</p>
    <h1>כתבים ממקור לטיני</h1>
    <p>במסורת הלטינית השתמרו תרגומים ועיבודים של חיבורים אפוקריפיים.</p>
  </header>

  {% set langSlug = "latin" %}
  {% include "language-books.njk" %}

  <nav class="bible-sections-nav" aria-label="ניווט בין שפות מקור">
    <a href="{{ '/texts/' | url }}" class="bible-sections-nav__back">← אל הספרייה</a>
    <div class="bible-sections-nav__sections">
      {% for otherSlug, otherLang in languages %}
      {% if otherSlug != langSlug %}
      <a href="{{ ('/texts/' + otherSlug + '/') | url }}" class="bible-sections-nav__link">{{ otherLang.label }}</a>
      {% endif %}
      {% endfor %}
    </div>
  </nav>
</div>
