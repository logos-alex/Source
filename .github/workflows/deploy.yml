name: Deploy to GitHub Pages

on:
  push:
    branches: ["main", "work"]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build site
<<<<<<< HEAD
        run: npm run build

      - name: Check for release content (if script exists)
        run: npm run check:release-content --if-present
=======
        run: |
          if node -e "const s=require('./package.json').scripts||{}; process.exit(s.build?0:1)"; then
            npm run build
            npm run --if-present check:release-content
            if ! node -e "const s=require('./package.json').scripts||{}; process.exit(s['check:release-content']?0:1)"; then
              echo "check:release-content script not found; skipped optional release-content validation"
            fi
          else
            echo "Missing npm script: build" >&2
            npm run
            exit 1
          fi
>>>>>>> cad5b50915efbed171b250bada5983667da53930

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./_site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Rebuild site for gh-pages mirror
        run: npm run build

      - name: Mirror to gh-pages branch (branch-source fallback)
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./_site
          force_orphan: true

      - name: Deploy to GitHub Pages (Actions source)
        id: deployment
        uses: actions/deploy-pages@v4
